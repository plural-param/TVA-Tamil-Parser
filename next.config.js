/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/tamilparser",
    assetPrefix: "/TamilParserAssetStaticPrefix",
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
